{
  "name": "smartattend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=@smartattend/shared",
    "build:api": "npm run build --workspace=@smartattend/api",
    "dev:api": "npm run dev --workspace=@smartattend/api"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}